# Technical Requirements

This hub owns actionable technical capability gaps and their status. It does not own current architecture descriptions, implementation details, product requirements, or secret values.

- `technical-requirements.md` - canonical `Pending implementation` backlog.
