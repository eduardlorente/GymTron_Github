# Implemented Capabilities Ledger

This document archives completed technical capabilities and remediations for GymTron. Active technical gaps and future requirements are tracked in `docs/requirements/technical-requirements.md`.

| Capability / Remediated Gap | Evidence | Impact | Required Outcome Achieved | Closure Status |
|---|---|---|---|---|
| Repository plaintext secret removal | Active credential values were removed from tracked Web/MAUI configuration, Docker scripts, publishing metadata, signing instructions, and the Google credential resource; ignored local templates/providers are documented. Values are omitted. | New commits no longer disclose replacement values through these paths. | Keep examples placeholder-only, enforce ignored local inputs, and prevent secret regressions through automated scanning. | Implemented |
| External credential rotation, revocation, history, and artifact assessment | Previously tracked database, publishing, signing, Google, and deleted MigratorDB credential material were assessed; repository owner confirmed external credential rotation, key revocation, and artifact assessment. Values are omitted. | Historical and distributed disclosure is mitigated by provider-side rotation and key invalidation. | Maintain provider-side secrets securely and prevent secret regressions through automated scanning. | Implemented (completed by repository owner) |
| Focused automated unit-test baseline | `tests/GymTron.UnitTests` uses xUnit and NSubstitute to verify Domain behavior and Application request, handler, validation, mapping, logging, and error paths across current features. `docs/testing/README.md` owns verified commands and boundary. | Core behavior has a fast regression baseline without runtime infrastructure. | Maintain behavior-first Domain/Application tests and expand them with approved behavior changes. | Implemented |
| Domain line and branch coverage | Coverlet 6.0.4 plus `tests/coverage.runsettings` measures intended production assemblies, and `eng/Assert-Coverage.ps1` validates unrounded Cobertura counts. All 47 tests pass with `GymTron.Domain` at 100% line (284/284) and branch (36/36) coverage. | Domain executable behavior has a measurable 100% line and branch regression gate. | Keep Domain at 100% line and branch coverage without broad exclusions or meaningless tests. | Implemented |
| Infrastructure coverage target | Integration tests (17 tests) cover all six DALs, repository behaviors, transaction rollback, isolation reset, and nullable mappings. Infrastructure coverage is 82.80% lines (496/599) and 79.17% branches (19/24), exceeding the 60/60 gate. CI workflow enforces Infrastructure 60%/60%. | Persistence behavior has a measurable regression gate with real-MySQL evidence. | Maintain integration test coverage as DAL/repository behavior changes; keep Infrastructure gate at 60%/60% or higher. | Implemented |
| Integration, persistence, frontend, and critical-flow test coverage | MySQL integration suite executed: 17 tests pass, covering all six DALs, repository behaviors, transaction rollback, isolation reset, and nullable mappings. MAUI UI automated testing is formally discarded by ADR-0005. | Persistence semantics have proven automated detection. UI quality is ensured via compiled bindings, typed contracts, and manual smoke testing. | Formally resolved by ADR-0005: discard fragile MAUI UI automation; rely on fast unit, integration, and API contract tests. | Implemented / Discarded by ADR-0005 |
| Focused Domain/Application CI validation | `.github/workflows/validation.yml` uses SDK selected by `global.json` on Ubuntu to restore from NuGet.org, build, test with Cobertura, enforce Domain 100%/100% and Application 80%/80%, and upload results. | Unit-test and coverage boundary has a reproducible CI gate without requiring MAUI workloads or runtime infrastructure. | Keep workflow aligned with checked-in test and coverage assets. | Implemented |
| Broader CI coverage | Focused workflow does not build MAUI or Web. MAUI workloads and runners are decoupled from PR CI by ADR-0005. Dedicated release workflow handles packaging on demand. | CI remains fast, headless, and cost-effective on Linux runners while release pipeline handles artifact creation. | Formally resolved by ADR-0005: PR CI stays streamlined on headless Linux; MAUI packaging isolated to tag releases. | Implemented / Streamlined by ADR-0005 |
| Formatting and static analysis baseline | Repository `.editorconfig` enforces consistent code style. `Directory.Build.props` applies common settings (`TreatWarningsAsErrors`, `EnforceCodeStyleInBuild`). All projects reference SonarAnalyzer.CSharp and Meziantou.Analyzer. Documentation added to `docs/language/csharp.md`. | Style and defect detection are consistent across all workstations and CI. | Maintain `.editorconfig` rules; review analyzer findings periodically. | Implemented |
| Dependency, security, and secret scanning | CI workflow includes two security jobs: (1) `secret-scanning` runs gitleaks on push/PR; (2) `dependency-scanning` runs `dotnet list package --vulnerable`. `.gitignore` cleaned of RedArbor.Coral noise (295 → 45 lines). | Known vulnerabilities and credential regressions are detected before merge. | Maintain scanning configuration; review and triage findings periodically. | Implemented |
| Safe configuration contract | Web uses User Secrets/provider overrides; Docker uses external environment input; `GymTron.App` decoupled from MySQL via `GymTron.Api` HTTP client. No database connection strings or credentials exist in mobile client. | Database credentials cannot be extracted from mobile binaries or client configuration. | Preserve API boundary; mobile app remains an HTTP consumer. | Implemented |
| Architecture and dependency enforcement | Permitted layer dependency directions, Domain package policies, and composition root exceptions formally defined in ADR-0004. NetArchTest and reflection assertions in `tests/GymTron.UnitTests/Architecture/ArchitectureTests.cs` enforce boundaries. | Dependency drift is caught automatically at build and test time before merge. | Maintain architecture tests as new projects or layers are introduced; update ADRs if boundaries evolve. | Implemented |
| Cancellation propagation | Repository, DAL interfaces, implementations, and Dapper calls accept and forward `CancellationToken` end to end; handlers pass token from MediatR through to persistence. | Long-running I/O can observe caller cancellation through persistence path. | Keep token threaded through new repository/DAL methods and add coverage when new I/O paths are introduced. | Implemented |
| Docker bootstrap validation | `docker-compose.yml` uses environment variables for credentials. `StartDockerScript.ps1` fixed to read from `.env` or environment variables, uses `docker compose` (v2), and validates `MYSQL_ROOT_PASSWORD` before starting. | Local setup can be reproduced without hardcoded secrets. | Keep script aligned with compose contract. | Implemented |
| ADR process | ADR template created (`docs/decisions/TEMPLATE.md`). Process documented in `docs/decisions/README.md` (lifecycle, numbering, review authority, registry). ADRs 0001 through 0005 created. | Cross-cutting decisions are recorded and discoverable. | Maintain ADR registry; create ADRs for significant architectural decisions. | Implemented |
| Release, rollback, and deployment verification | Release workflow (`.github/workflows/release.yml`) builds signed Android APK and publishes Web artifacts on tag push (`v*`). GitHub Release created automatically with artifacts and release notes. Rollback procedure documented in `docs/infra/release.md`. | Releases are reproducible and reversible with documented procedures. | Maintain release workflow; verify APK and Web artifacts on each release. | Implemented |
| License artifact confirmation | MIT license file (`LICENSE`) added with copyright notice for Eduard Lorente. README references MIT license. | Distribution rights and obligations are backed by canonical artifact. | Maintain license file; update copyright year as needed. | Implemented |
| Dormant Google integration disposition | Google Sheets integration removed: 3 Google packages, commented source files, and unused helpers deleted. MySQL is sole persistence backend. | Dead dependencies and dormant source no longer confuse supported integrations. | MySQL is sole persistence backend. | Implemented (decision: removed) |
| Transaction isolation and reset semantics | `QueryExtensions.ToReadUncommited()` wraps SQL text with session isolation statements. Integration tests confirm that MySQL's `SET TRANSACTION` (without SESSION) only affects next single transaction and reverts automatically. | Isolation behavior is proven under success, failure, and connection reuse. | Keep reset statement as defensive coding. | Implemented |
| Legacy namespace typo resolution | `GymTron.Insfrastructure` renamed to `GymTron.Infrastructure` across solution (folder, project file, namespaces, CI workflows, and documentation). | Naming is standard English and consistent across all layers. | Maintain standard naming; automated architecture tests enforce boundaries. | Implemented |
| Stale solution reference cleanup | Removed `4 - GitHub` solution folder and reference to non-existent `copilot-instructions.md` from `GymTron.sln`. | IDE users no longer see broken solution items. | Keep solution file aligned with actual repository structure. | Implemented |
| Operational APK instruction safety | `Instruccions generar APK.txt` directs signing inputs to `GymTron.Signing.local.props` (ignored file) or CI secrets. No password hints or values present. | Contributors have safe boundary for local signing material. | Maintain value-free instructions. | Implemented |
| Application validation coverage and execution | FluentValidation covers all commands and queries with input parameters. MediatR pipeline behavior (`ValidationBehavior`) executes registered validators before handlers. | Invalid requests receive consistent treatment across all entry points; validation failures caught before handler execution. | Keep validators aligned with request surface changes. | Implemented |
| Repository contract completeness | `ILogRepository`, `IBodyWeightRepository`, `IExerciseRepository`, and `IRoutineRepository` no longer inherit `IEntityRepository`; unsupported members removed. | Public abstractions no longer promise operations they cannot perform. | Keep repository contracts aligned with actual Application usage. | Implemented |
| Transactional atomicity for multi-step writes | `RoutineDAL.Create`/`Update` run parent mutation, item deletion, and item inserts inside one connection and transaction with commit/rollback. Integration tests prove rollback behavior. | Mid-write failure no longer leaves partially created or replaced routines. | Maintain transaction tests as routine write behavior changes. | Implemented |
| MAUI asynchronous lifecycle and exception safety | View models use `async Task` methods with `IsBusy` guards and try/catch error handling instead of `async void`. | Exceptions are caught and displayed via `DisplayAlert`. Loading state properly managed. | Maintain async/await pattern in new view models. | Implemented |
| MAUI binding correctness | All XAML pages with bindings use `x:DataType` for compiled bindings. Color properties notify changes. Nested `DataTemplate` models immutable. | Binding errors detected at compile-time. UI refresh behavior is reliable and consistent. | Maintain compiled-binding coverage when adding new pages. | Implemented |
| MAUI navigation and service resolution consistency | Navigation uses `Shell.Current.GoToAsync()` consistently. Modals receive dependencies via constructor injection; static `App.Services` service locator eliminated. | Navigation state, object lifetime, and DI boundaries are consistent. | Centralized navigation and constructor injection maintained. | Implemented |
| UI localization and encoding | Complete localization infrastructure implemented for MAUI and Web. Supported languages: Catalan (default), Spanish, English. Encoding issues (mojibake) resolved. | Users can use application in preferred language. | Maintain resource files when adding new UI text. | Implemented |
| Web backup-source cleanup | Removed `Edit_Backup.cshtml` and `Edit_New.cshtml` from `src/GymTron.Web/Pages/Routines/`. Only `Edit.cshtml` remains. | No stale backup files confuse maintenance. | Use version control for history. | Implemented |
| Time semantics and clock ownership | Domain uses `IClock` interface for all time operations. `SystemClock` provides production implementation using `DateTime.UtcNow`. `FakeClock` enables deterministic testing. All timestamps stored in UTC. | Time handling is consistent, testable, and timezone-aware. | Use `clock.UtcNow` for all persistence. | Implemented |
| Domain exception consistency | Domain defines `DomainException` as base for expected business failures, with `EntityNotFoundException` and `InvalidDomainOperationException`. `GymTron.Web` maps these to `ModelState`; `GymTron.Api` maps to RFC 7807 `ProblemDetails`. | Callers reliably distinguish expected domain rejection from unexpected system failure. | Use domain exceptions for state transitions and business rules. | Implemented |
